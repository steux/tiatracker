globalSpeed:
- Adapt replayer
- Adapt bpm display (switch off for localSpeed?)
- Add pattern tempo display? (In name?)
- When moving the cursor, adapt even/odd speed gui element
- Adapt info tab
- TrackTab::updateTrackTab: Check if we need to distinguish between globalTempo true/false for setting speeds
- Implement export
- Update k65 player
- Change version display to 1.2
- Update manual
- Adapt timestamps display
- Adapt trackTab::updateTrackStats()
- TrackTab::updateTrackStats(): Set even/odd speed if globalTempo is enabled
- TrackTab::setEven/OddSpeed: What happens if editPos for channel 1 is greater than maxEditPos for channel 0? Catch!
- load: globalSpeed etc., update GUI
- when global speed checkbox changes, update speed values in GUI!
- duplicate pattern: odd/even local speeds?
