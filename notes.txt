globalSpeed:
- When creating a new pattern, use global even/odd values for init
- Adapt replayer
- Adapt bpm display (switch off for localSpeed?)
- React on checkBoxGlobalSpeed (update gui at least)
- Add pattern tempo display? (In name?)
- When moving the cursor, adapt even/odd speed gui element
- When even/odd speed values change, adapt pattern values accordingly (left channel only?)
- Adapt into tab
- Update globalSpeed GUI on song load
- Implement export
- Update k65 player
- Change version display to 1.2
- Update manual
